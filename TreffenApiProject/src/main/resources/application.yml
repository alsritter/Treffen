spring:
  profiles:
    active: '@profiles.active@'   # 这里引用的是 pom.xml 中配置的 key
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/?useUnicode=true&characterEncoding=utf8&useSSL=true&useServerPrepStmts=true
    username: root
    password: root
